# PDF 处理
pdf2image>=1.16.0
PyMuPDF>=1.23.0

# 视觉模型 SDK（根据需要选择安装）
zhipuai>=2.0.0          # GLM-4V 模型
openai>=1.0.0           # GPT-4V 模型
# anthropic>=0.8.0      # Claude-3 模型（可选）

# 图像处理
Pillow>=9.0.0

# 网络和异步
requests>=2.28.0
aiohttp>=3.8.0

# 说明：
# 1. 系统依赖：需要安装 poppler
#    - Ubuntu/Debian: sudo apt-get install poppler-utils
#    - macOS: brew install poppler
#    - Windows: 下载并安装 poppler for Windows
#
# 2. 环境变量配置：
#    创建 .env 文件并添加：
#    GLM_API_KEY=your_glm_api_key_here
#    OPENAI_API_KEY=your_openai_api_key_here
